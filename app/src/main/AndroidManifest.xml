<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/genius"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.tutor.EditCourse"></activity>
        <activity android:name=".ui.tutor.AddCourse" />
        <activity android:name=".ui.tutor.TutorCourse" />
        <activity android:name=".ui.tutor.add_course" />
        <activity android:name=".ui.tutor.course_tutor.course_add" />
        <activity android:name=".ui.course.courseContent.LogicGates" />
        <activity android:name=".ui.course.courseContent.ComputerScience" />
        <activity android:name=".CourseContent" />
        <activity android:name=".Course_Content" />
        <activity android:name=".course_content" />
        <activity
            android:name=".MainActivity"
            android:label="@string/genius">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>